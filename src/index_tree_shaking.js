import { add, mult, promiseTimeout } from "./utils";
console.log("add(4, 3)", add(4, 3));
console.log("mult(2, 2)", mult(2, 2));
// const thing = async () => await promiseTimeout(resolve => resolve(3));
